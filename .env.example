# 신규 시스템 PostgreSQL (core/stg/asset 스키마)
# docker-compose.yml 로컬 DB 사용 시 아래 값 그대로 사용 가능
DATABASE_URL="postgresql://jipdarie:jipdarie@localhost:5432/jipdarie?schema=public"

# UPIS 레거시 DB 접속 (직접 접속 방식일 때만, read-only 권장)
# 커밋 금지 — .env 는 .gitignore 처리됨
UPIS_DATABASE_URL=""
